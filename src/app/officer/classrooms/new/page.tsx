import { CreateClassroomView } from "@/components/classrooms/create-classroom-view";

export const metadata = { title: "Create Classroom" };

export default function CreateClassroomPage() {
  return <CreateClassroomView />;
}
